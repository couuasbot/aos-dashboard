{
  "name": "aos-dashboard",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w packages/aos-core && npm run dev -w apps/api & npm run dev -w apps/web",
    "build": "npm run build -w packages/aos-core && npm run build -w apps/api && npm run build -w apps/web",
    "test": "npm run test -ws",
    "lint": "npm run lint -ws"
  },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
